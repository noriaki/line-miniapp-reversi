# LINE Front-end Framework (LIFF) Configuration
# Copy this file to .env.local and fill in your values

# =============================================================================
# Required Variables
# =============================================================================

# LIFF ID (Required)
# Obtain from LINE Developers Console: https://developers.line.biz/console/
# Format: [channel-id]-[random-string]
# Example: 1234567890-abcdefgh
NEXT_PUBLIC_LIFF_ID=your-liff-id-here

# =============================================================================
# Optional Variables
# =============================================================================

# Base URL (Optional, Server-side only)
# Explicitly set the application base URL for OG images and share links.
# If not set, falls back to VERCEL_PROJECT_PRODUCTION_URL (on Vercel) or localhost:3000 (local).
# Use this when deploying to non-Vercel platforms or using custom domains.
# Format: domain only, without protocol (https:// is added automatically)
# Example: your-domain.com
# BASE_URL=

# =============================================================================
# System Variables (Do not set manually)
# =============================================================================
# The following variables are automatically set by their respective platforms:
# - VERCEL_PROJECT_PRODUCTION_URL: Auto-injected by Vercel (always production URL)
# - CI: Auto-injected by CI/CD platforms (GitHub Actions, etc.)
