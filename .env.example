# LINE Front-end Framework (LIFF) Configuration
# This file contains example environment variable settings for LIFF integration

# LIFF ID
# Obtain this from LINE Developers Console (https://developers.line.biz/console/)
# Format: [channel-id]-[random-string]
# Example: 1234567890-abcdefgh
NEXT_PUBLIC_LIFF_ID=your-liff-id-here

# LIFF Endpoint URL (for Flex Message buttons in share feature)
# This is the LINE MINI App or LIFF app URL that users tap to launch the app
# Example: https://miniapp.line.me/xxxx-xxxx or https://liff.line.me/xxxx-xxxx
NEXT_PUBLIC_LIFF_ENDPOINT_URL=https://miniapp.line.me/your-app-id

# =============================================================================
# Cloudflare R2 Configuration (Server-side only)
# =============================================================================
# These variables are used by the API Route (/api/upload/presigned) for
# generating presigned URLs to upload share images.
#
# How to obtain:
# 1. Go to Cloudflare Dashboard (https://dash.cloudflare.com/)
# 2. Navigate to R2 > Overview
# 3. Create a bucket (if not exists)
# 4. Go to "Manage R2 API Tokens" to create an API token with read/write access
#
# R2 S3-compatible API endpoint
# Format: https://<account-id>.r2.cloudflarestorage.com
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# R2 API Token credentials (from "Manage R2 API Tokens")
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key

# R2 bucket name
R2_BUCKET_NAME=your-bucket-name

# R2 public URL for serving uploaded images
# This can be either:
# - R2 public bucket URL: https://pub-<hash>.r2.dev
# - Custom domain connected to R2 bucket
R2_PUBLIC_URL=https://pub-xxxx.r2.dev
